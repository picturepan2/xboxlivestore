block ad-adsense
  .docs-ad
    .hide-md.text-center
      script.
        ( function() {
          if (window.CHITIKA === undefined) { window.CHITIKA = { 'units' : [] }; };
          var unit = {"calltype":"async[2]","publisher":"picturepan2","width":550,"height":250,"sid":"Chitika Default"};
          var placement_id = window.CHITIKA.units.length;
          window.CHITIKA.units.push(unit);
          document.write('<div id="chitikaAdBlock-' + placement_id + '"></div>');
        }());

      script(type="text/javascript", async='', src="//cdn.chitika.net/getads.js")